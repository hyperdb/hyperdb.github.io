name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"] # ★デフォルトブランチがmasterの場合は 'master' に変更

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm" # pnpmやyarnを使っている場合は適宜変更

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./dist" # Viteのデフォルト出力フォルダ

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
